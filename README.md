# flask-skeleton
[INCOMPLETE]
Sample microservice skeleton for Flask

To run locally:

```
$ cd <PROJECT_DIR>
$ export FLASK_APP=app.py
$ export FLASK_ENV=development
$ pip install -e .
$ flask run
```
